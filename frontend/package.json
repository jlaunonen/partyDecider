{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build --mode preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "generate-api": "openapi-generator-cli generate -g typescript-fetch -t openapi-overrides/ -o src/api/ -i http://127.0.0.1:8192/openapi.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.0.3",
    "@tsconfig/svelte": "^3.0.0",
    "svelte": "^3.55.1",
    "svelte-check": "^2.10.3",
    "tslib": "^2.5.0",
    "typescript": "^4.9.3",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.3",
    "svelte-routing": "^1.8.0"
  },
  "optionalDependencies": {
    "@openapitools/openapi-generator-cli": "^2.6.0"
  }
}
